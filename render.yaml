services:
  # Frontend: React (Static Site)
  - type: web
    name: nextrole
    runtime: static
    rootDir: frontend
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    envVars:
      - key: VITE_API_URL
        sync: false
