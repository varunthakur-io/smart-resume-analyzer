{
    "name": "smart-resume-analyzer",
    "version": "1.0.0",
    "scripts": {
        "dev:backend": "cd backend && .\\venv\\Scripts\\python.exe app.py",
        "dev:frontend": "cd frontend && npm run dev",
        "dev": "concurrently -n BACKEND,FRONTEND -c blue,green \"npm:dev:backend\" \"npm:dev:frontend\""
    },
    "devDependencies": {
        "concurrently": "^9.1.2"
    }
}